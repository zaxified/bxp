#!/usr/bin/env bash
# Run desktop-side tests: flutter analyze + flutter test for bxp-gui plus
# Dart unit tests for the embedded json5_ast package.
#
# Stub — real content lands in Phase 9 of the release-split plan. Until
# then we no-op cleanly so the top-level wrapper works.

set -e
echo "test-desktop.sh: not yet implemented — Phase 9 placeholder"
exit 0
